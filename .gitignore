# game binaries (user-supplied) + build artifacts
*.apk
*.elf
*.nacp
*.nro
*.so
assets/
build/
config.txt
debug.log
log.txt
portmaster/_so/

# the built Linux binary + debug captures
/ct
/ct_pm.zip
/movie_probe
portmaster/pkg/ct/ct
*.ppm
frame_*
.DS_Store

# bundled FFmpeg libs (build artifacts — reproduce via portmaster/ffmpeg-build.sh)
portmaster/pkg/ct/libs.aarch64/

# port screenshot (generated from the user's own game via CT_CAPTURE)
portmaster/pkg/ct/screenshot.png
